apiVersion: v1
kind: ServiceAccount
metadata:
  name: mbsrn-api
  namespace: mbsrn
  # Workload Identity mapping is environment-specific and must be set in-cluster.
  # Example:
  # annotations:
  #   iam.gke.io/gcp-service-account: mbsrn-api-runtime@<PROJECT_ID>.iam.gserviceaccount.com
